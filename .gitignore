# 生圖的中間版本：每次嘗試都是 2–3MB 的 PNG，網站不會用到，
# 留在 ~/taiwan-people-drafts/ 供比對，不進版控
img/*.png
# 簽名是網站要用的資產，不是生圖的中間版本，要進版控
!img/signature.png
!img/signature@2x.png
img/*-v[0-9].webp
img/*-v[0-9][0-9].webp

# 產線暫存
/tmp/
*.tmp
review/
img/*.prompt.txt
output/

# 候選圖：gen.sh 會在輸出檔旁開 <名稱>.cand/ 放全部候選，一次 2-3MB×N。
# 上面那條 img/*.png 只擋得住 img/ 直下的檔案，擋不住子目錄，
# 結果 123MB 的候選差點進版控。審完的候選留在 ~/taiwan-people-drafts/。
img/*.cand/

__pycache__/
*.pyc

# 角色母檔。img/*.png 只擋 img/ 直下的檔案，擋不到子目錄，
# 這幾張 2046×3074 的 PNG（一張 3MB）差點跟著進版控。
# 網站要的是 img/hero.webp 與 img/hero@2x.webp，母檔留在磁碟上供重出用。
img/alui/
