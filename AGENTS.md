# 動這個 repo 之前先讀這頁

## 先讀哪裡

| 要做什麼 | 先讀 |
|---|---|
| 生成或修改角色圖 | `data/character.md` |
| **畫任何族群的服飾** | **`data/costume.json`** |
| 寫任何縣市的文案 | `data/counties.json` |
| 改網站結構或樣式 | `index.html`（單檔，含資料表、地圖路徑、全部樣式） |
| 改服飾依據頁 | `costume.html`（單檔，資料用 fetch 讀 `data/costume.json`） |
| 更新新住民數字 | 跑 `tools/fetch_immigration_stats.py`，再跑 `sync_split.py`。**不要手改數字** |
| 更新原住民人口 | 跑 `tools/fetch_indigenous_stats.py`。**不要手改數字** |
| 改人口脈絡頁 | `population.html`（單檔，資料用 fetch 讀 `data/indigenous.json` 與 `data/newcomers.json`） |

## 硬規則

**一、不要抄原始海報上的字。**
海報由 AI 生成，已知有錯：高雄那張的「代表樹 鳳凰木」「代表鳥 黑面琵鷺」
其實是台南的，「代表海產 石敢・櫻搏」不是詞。
所有文字以 `data/counties.json` 為準，該檔用 `source` 欄位標可信度。

**海報的文案也一樣不能照抄。** 那些字是 AI 生成的，會有機器中文的語病：
台北那張寫「每一天都發生未來」——未來不會被「發生」，那是把主謂關係寫成動賓。
桃園、台東那兩句抽出來之後會缺主詞，「也是」「更是」沒有東西可承接。
彰化的「希望城市」、雲林的「雲林上場」則是搭配生硬。

所以正確的做法是**取材於原文，但要當國文稿子審過**：
語法錯的改對、缺主詞的補回、生硬的換掉、全稿重複的用詞分散開。
改了什麼與為什麼，寫進該筆的 `quote_basis`。

**一之一、普通版一律當代穿著，傳統服飾只出現在特別版。**
`data/counties.json` 的一筆紀錄有沒有 `culture` 欄，決定它在宣稱什麼：

| | 宣稱 | 服裝 |
|---|---|---|
| 無 `culture`＝**普通版** | 「阿蕊在這個地方」 | 當代日常＋在地產業或地景元素。**不穿任何族群的傳統服飾** |
| 有 `culture`＝**特別版** | 「阿蕊穿某族的服飾」 | 逐項對回 `data/costume.json`，`scene.costume_basis` 交代每一項 |

這條沒有例外，客家也一樣：新竹縣與苗栗原本讓普通版穿北客大襟衫，
雖然那是有依據的、也沒有階級紋樣的問題，但**普通版一旦穿了任何一族的傳統服飾，
就等於用那一族代表整個縣市**。兩筆已移到 `新竹縣・客家`、`苗栗縣・客家` 兩個特別版。

**特別版在網頁上要標明族群名與依據連結。** 主標可以是「台東・特別版」這種產品層級的分類，
但底下一行必須寫清楚是哪一族、依據在哪。不標族名就會退回花蓮那個病灶
（有織紋但不指名族群＝用一族代表全縣），依據藏起來則等於整個資料集白做——
外人無從檢驗，就回到「AI 亂編」的指控。含混不是保守，是把責任推給看的人。

原本花蓮、台東、屏東、南投四筆是「依原海報保留原住民族織紋但**不指名族群**」。
那不是折衷，是**畫出一件不屬於任何族的族服**——光花蓮就有阿美、太魯閣、撒奇萊雅、
噶瑪蘭、賽德克、布農六族，太魯閣的美學是白底單獨菱紋的留白，撒奇萊雅是土金與暗紅，
混成一件「泛原住民風」等於六族都畫錯。這四筆已改成當代日常。

新竹縣與苗栗縣原本寫「客家藍衫（立領盤扣、白色小花印花）」，三處都錯：
印花是把 1960 年代的花布誤當客家傳統、盤扣是對襟的做法（大襟衫用直布扣）、
藍衫是南部六堆的稱法（桃竹苗屬北客，該叫大襟衫）。已依客委會的調查改對。
苗栗那條「藍花頭巾←客家」查不到官方依據，已刪。

`build_prompt.py` 會擋：服裝欄出現族群織紋或族服名稱，但 `scene.costume_basis` 是空的，
就不給產 prompt。這條**抓到我手動盤點時漏掉的苗栗縣**——所以不要靠眼睛掃，靠腳本。

**一之二、族群服飾一律以 `data/costume.json` 為準，不准憑印象畫。**
台灣沒有一種叫「原住民風」的東西。光台東就有阿美、卑南、排灣、布農、達悟五族，
各族之內還分群，同一族不同群的服飾可以完全不像。畫面上出現的織紋若指不出是哪一族哪一群，
那就是拼貼，不是設計。

該檔的 `_redlines` 是五條會出事的地方，開工前讀完：階級紋樣是身分不是裝飾、
熊鷹羽毛同時是保育問題、織紋可能是登記在案的智慧創作專用權、
「客家花布」不是客家傳統服飾、不要用「原住民風」含混帶過。

每一族有 `checklist`（出圖後逐項對照）與 `pitfalls`（已知會畫錯的地方），
出圖之後照 checklist 走一遍，而且要寫出檢查結果。`gaps` 欄寫的是查不到什麼，
**那一欄有東西就代表不要自己補**，例如馬卡道族與馬祖目前沒有可靠依據，就不要畫。

改這個檔要跟著更新 `data/sources/` 的原文存檔，並在該筆的 `sources` 補上出處 id。
沒有出處的內容不要寫進去。

**一之三、新住民寫進地方，不寫進衣服。**
到 115 年 6 月底，全台累計 62 萬人以婚姻移民的身分申請在台居留或定居，
其中 31.1 萬人已歸化或取得定居資格——法律上就是國民，剛好是總數的一半。
量級與原住民族全體相當（63.8 萬），這個站不能當作沒有這些人。

這兩個數字的寫法有三條規矩，在本節末尾，寫任何相關文案之前先看過：
累計數不是現住人口、「已歸化」不等於「已經是國民」、兩份統計不能相加也不能比大小。

但**不要把他們做成特別版、不要把越南或印尼的服飾寫進 `data/costume.json`**，三個理由：

1. **舉證強度跟不上。** 這個檔的門檻是對得回官方機關、公立博物館或受委託的學術調查。
   áo dài、kebaya 的權威出處在越南與印尼，不在台灣的檔案系統裡。用低一級的證據
   收進同一個檔，整份資料集的可信度一起掉。
2. **分類是錯的。** 「越南」是國籍不是族群。越南官方認定 54 個民族，áo dài 是京族的；
   印尼超過 1,300 個族群。把「東南亞服飾」當一個類別，跟一之二拒絕的「原住民風」
   是同一個錯誤，只是換了半球。
3. **命題方向會反掉。** 普通版的宣稱是「阿蕊在這個地方」，穿當代日常；
   新住民在台灣的日常穿著本來就是當代日常。給她穿上 áo dài 等於在說
   「你要穿得像外國人，我才認得出你是新住民」，那是把人異國情調化。

所以做法是：卡片上多一行「住一起」，只寫**人數與來源地**，數字直接來自移民署統計表，
底下標明依據與期別。這一行是在說這個地方住著誰，不是把人變成地方名產，
也不宣稱他們長什麼樣、穿什麼。

標籤用「住一起」不用「一起住」，有兩個理由。一是節奏：同一組的「走一趟」「吃一輪」
都是動詞＋補語，「一起住」是副詞＋動詞，唸起來會斷掉。二是語氣：「一起住」帶邀請的
口氣（「要不要一起住」），但這張表是民國 76 年起累計的，裡面很多人已經住了二三十年、
小孩都成年了。**這一行講的是既成事實，不是歡迎剛到的人**——寫成「歡迎新住民」那類
句子，等於把住了三十年的鄰居又推回門口。

這條也框住了這一區塊的範圍：它講的是人，不是人口結構。原住民人口與總人口只進
`population.html` 備查，不上卡片、不算佔比、不做縣市排名。

**原住民人口與新住民人數不是同一種量，永遠不要相加、也不要直接比大小。**
原民會那份是現住登記人口（此刻依《原住民身分法》登記在案的人），
移民署那份是 76 年起的累計申請人數。一個是存量、一個是流量累計，
加起來不對應任何真實人數，相減也算不出誰多誰少。並排只能各看各的量級。

**原住民那一欄不含平埔族群，寫台南的文案時要特別小心。**
《平埔原住民族群身分法》114 年 10 月 17 日三讀，西拉雅等 9 個族群已申請、審議中，
尚未完成認定，所以不在原民會的統計裡。而 `data/sources/pingpu/` 收了西拉雅的資料——
講服飾的時候認、講人口的時候不認就是矛盾，頁面上已寫明，審議有結果要回頭更新。

**各縣市總人口目前查不到能腳本化的來源，所以沒有放。**
戶政司在政府資料開放平臺的縣市資料集停在 107 年、村里月報停在 109 年 8 月；
內政統計月報表 1.7 公開的歷史檔只到 105 年 12 月，當期改由查詢系統提供，
沒有固定網址的檔案可抓。`population.html` 的「還缺什麼」一節照實寫出來了。
要補的話得先找到可直連的檔，不要手抄一個數字上去。

**那張表是累計數，不是現住人口，寫文案前先記住這件事。**
期別是民國 76 年 1 月至 115 年 6 月底，將近四十年的累計，而且表頭自己註明
「本表係申請人數且不一定在台」。期間過世的、離婚返回原籍的、取得身分後遷居國外的，
都沒有從累計數裡扣掉。所以不能寫成「全台新住民 62 萬人」那種現在式，
拿去跟原住民族人口（現住統計）相比也只能講量級、不能講誰多誰少。

**「已歸化」不等於「已經是國民」，主詞一定要寫出來。**
《國籍法》第 3 條的歸化對象是外國人或無國籍人；大陸地區人民依兩岸條例、港澳居民依
港澳條例，走的是定居設籍那條線，取得身分之後也永遠不會進到歸化的數字裡。
`naturalized` 這個欄位只涵蓋外裔外籍配偶，寫成「其中 N 人已歸化為國民」會讓讀者
以為剩下的都還不是國民——實際上光是陸配拿到定居證的就有 14.8 萬。

所以要講「已經取得我國身分的人」，一律是三個欄位相加：
`naturalized`（外籍歸化）＋ `prc_settled`（陸配定居證）＋ `hk_macau_settled`（港澳定居證）。
到 115 年 6 月是 14.5＋14.8＋1.8＝31.1 萬，剛好是總數 62 萬的一半；
只寫 `naturalized` 會少算一半。

用詞只能寫「已歸化或取得定居資格」，不能簡稱「已歸化」——歸化取得的是國籍、
定居取得的是設籍資格，兩者是不同的法律事件，合稱不能挑其中一個當代表。

要寫進 `counties.json` 的文案也照同一條線：寫他們的生活與所在，不要把人寫成
「在地產業元素」跟蚵仔、茶葉並列。移工是另一回事——87 萬人、居留性質完全不同、
絕大多數不是移民，不要跟新住民混在一起講。

## 一個縣市可以有多張

`counties.json` 的 `counties[]` 同時放普通版與特別版，靠 `name` 區分
（`台東縣` 與 `台東縣・卑南族`）。這樣 `gen_all.sh`、`verify_base.py`、
`register_base.py`、`review.py`、`pick.py` 一行都不用改——它們本來就只認 `name` 與 `id`。

特別版那一筆只寫要覆寫的欄位，地標、市花、視點、光向全部由 `build_prompt.py`
從所屬縣市繼承。不繼承的話每加一筆就要整份抄一次，抄錯了就是又一個
「憑印象覆蓋既有資料」的案例。

```bash
python3 tools/build_prompt.py 台東縣・卑南族              # 產 prompt
python3 tools/build_prompt.py 台東縣・卑南族 --checklist   # 出圖後照這份驗收
```

分工是刻意的：**穿哪幾件由人挑**（寫進 `scene.outfit`，用 `costume_basis` 交代依據），
**色彩、紋樣、階級限制由腳本從 `costume.json` 灌進 prompt**。
逐部位的完整清單不進 prompt，因為那份清單含日常／勞動／年長等替代選項
（卑南族年長女性改穿長褲），整份倒給模型只會讓它把不同情境的東西畫在一起。

`sync_split.py` 只同步普通版，地圖上仍然只有 22 個可點區域。
**`gen_all.sh` 不帶參數會把沒有 `base` 的特別版一起排進生成佇列**，這是預期行為。

別窮舉。扣掉幾乎全島的閩南漢人，縣市 × 族群有 64 個配對（台東 9、高雄 8、花蓮 7），
每張都要走完驗收才算數。做法是每個縣市保留一張普通版，需要時再加特別版。

**二、底圖裡不能有任何文字。**
縣市底圖（`img/<id>-base.webp`）只有場景與人物。
上緣 22% 與右緣 40% 必須留空（天空與海），那是給 HTML 疊字用的。
文字全部由 HTML 或 canvas 疊上去，改字不重生圖。

**三、任何對外文字的主詞都是地方，不是人。**
寫「高雄，代表了 熱情・豪爽・打拚」，不要寫「高雄**人**代表了⋯」，
也不要用「這裡的人」這種以人為主詞的標籤。
後者是對幾百萬人的人格描述，正面標籤仍然是標籤。
雙關活在系列名「台灣人代表」裡，不必在每張圖上寫成斷言句。

這條**適用於分享圖、卡片、網頁文案全部**。
原本只寫「分享圖的」，結果補卡片那列時標籤寫成「這裡的人」，主詞又變回人——
範圍寫窄了就會從沒涵蓋到的地方漏出去。

**四、頭上那朵花＝該縣市的市花。這一條只適用於普通版。**
這是角色的識別記號，不是裝飾。詳見 `data/character.md`。

**特別版的頭飾先看族群有沒有記載。** 有記載的就畫記載的：拉阿魯哇族女性是黑色帽飾插公雞羽毛髮髻加紅黃綠白藍彩帶、邵族女性是花草頭冠、鄒族女性是黑色頭巾纏髮。
**沒記載的才沿用縣市花**——那是阿蕊的識別記號，不因為換成特別版就拿掉。
目前沿用縣花的有：太魯閣、排灣北排平民、賽夏、泰雅馬里闊丸群、噶瑪蘭。

**但族群明文限制的東西，縣花補位也不能違反。** 排灣那筆戴九重葛可以，戴任何羽冠都不行——熊鷹羽頭冠是頭目與貴族專屬，且熊鷹是瀕臨絕種保育類。

**髮型多數族群沒有記載，沿用角色卡的丸子頭即可。** 有記載的只有三族：拉阿魯哇（過去以頭巾纏髮、現今為黑色帽飾插公雞羽毛髮髻）、阿美（方形頭巾纏繞盤髮）、大武壠（頭布纏法有逐步記錄）。沒記載就不要自己發明族群髮式。
唯一例外：台南用鳳凰花，依據是**鳳凰樹＝台南市樹**（一樣是查證過的官方象徵）；
市花蝴蝶蘭同時是雲林、台南、台東三個縣市的，無法識別。
花蓮縣花是蓮花，蓮花是大型水生花，戴在頭上不合理但仍照用，未另創花冠。

**六、任何非花配件都必須對得回 `symbols` 或 `scene.landmarks` 的某一筆，對不回就拿掉。**
只寫個名字加上去的裝飾不算設計。已依此刪掉：台北 101 耳環（台北的 landmarks 沒有 101）、
新北金髮圈、新竹市金髮夾、台中彩色髮夾、屏東黃花、各縣市自己變色的流蘇。
留下來的每一筆都在 `scene.accessory_basis` 寫明依據。
苗栗那條「藍花頭巾←客家」原本標著待查，這次查了：客委會的服飾調查沒有頭飾專章，
也找不到台灣客家頭飾的官方專論，所以**已刪**。查不到就不畫。
目前只剩基隆水手帽←基隆港仍待查證。
髮型本身（馬尾／丸子頭）不是配件，不需要依據。

金珠流蘇髮簪是**阿蕊的固定配件**，二十二個縣市都一樣，不隨縣市變色——
它屬於角色卡，不屬於任何縣市。

**五、首頁不要動角色造型。**
首頁是台灣層級的，縣市頁才換在地造型。（這裡的「首頁」指 index.html 的第一段，
跟普通版／特別版是兩回事。）

## 已知風險：AI 畫不出植物學上正確的花

首頁那朵「通用白花」放大七倍檢視後，是拼接物不是真花：
花瓣約六片（梅、櫻、桃這類薔薇科都是嚴格五瓣）、瓣尖一半有櫻花式缺口一半是圓弧、
花心是熱帶花的放射狀喉部卻又點了幾根散落的雄蕊。

所以「髮上的花＝該縣市市花」這條規則要落實時，
等於要求模型畫出十八種**可辨識的真花**——木棉的五片厚瓣、天人菊的舌狀花序、
紅花石蒜的反捲細瓣。翻車機率很高，而且花畫錯在地人一眼看得出來，
跟市樹市鳥寫錯是同一類風險。

真的要做的時候，三條路擇一：

1. 每張生完逐一比對真花照片驗收，不對就重生
2. 花單獨生成、驗過再合成到髮上
3. 不宣稱物種精確，花保持風格化，種類靠文字說明（卡片上本來就寫著「市花 木棉花」）

目前**尚未實作**，首頁維持那朵不指定的白花。

## 生底圖的 prompt 陷阱

**不要寫「不要文字」。** 模型會把字挖掉但留下承載那些字的空白區塊——
高雄第一版的珍奶杯上就留了一塊沒有內容的米色矩形，看起來像髒污；
T恤與帆布袋的印花也只剩城市剪影，字被挖空。

正確寫法是**指名保留**：

> the cup keeps its printed paper label with 高雄 and KAOHSIUNG on it,
> the T-shirt keeps its printed skyline graphic with 高雄 KAOHSIUNG lettering,
> the tote keeps its printed 高雄 KAOHSIUNG graphic.
> Do not leave any empty rectangle, blank patch or unlabelled sticker.

另外手部要明講：`four fingers and a thumb, natural knuckles, no extra or fused fingers`。
第一版握杯的手有四根手指擠在杯子右側。

## prompt 一律由 `tools/build_prompt.py` 產生，不要手打

```bash
python3 tools/build_prompt.py 高雄市
```

高雄第一版的紅花畫錯，成因不是模型：是我手打 prompt 時憑印象寫了「鳳凰木」
（那是台南市樹），沒有對照旁邊那欄已經查證過的市花。腳本擋兩件事：

- 資料不全就退出（缺 landmarks／plants／hand／flower 任一項）
- 市花與 `scene.plants` 不一致就退出——那正是上面那個錯誤的形狀

資料錯要回頭改 `data/counties.json`，不能繞過腳本。

## 印花也要對得回資料，但不要為了統一而全部寫死

硬規則六（配件必須對得回 `symbols` 或 `scene.landmarks`）當初刪掉了台北 101 耳環、
新北金髮圈、台中彩色髮夾，卻**漏掉了衣物與提袋上的印花**。

新竹市的帽T、徽章、提袋全是電路板與 IC 晶片圖案——那對回的是科學園區，
而新竹市的 `landmarks` 是十八尖山、南寮漁港、新竹都城隍廟，沒有科學園區。
畫出來也像剪貼素材。已改成城隍廟的燕尾脊與石獅線描。

**但不要因此把 22 個縣市的印花全部寫死。** 實際看過 16 張提袋，
15 張自由發揮的都好看而且對得回資料（台北 101 線描、彰化廟宇、台南赤崁樓、
台東八拱橋、連江聚落、屏東珊瑚、其餘多是該縣市的市花），只有新竹市那張是壞的。
全部寫死等於把 15 張好的洗掉，那是為了統一而統一。

所以 `scene.tote_print` 是**選用覆寫**：不設就讓模型自由發揮，
畫壞或對不回資料的才寫死。目前只有高雄與新竹市有。

順帶修掉一個死欄位：`tote_print` 原本**只有 `--fix-tote-print` 讀得到，`build()` 沒用它**，
所以高雄那筆設了也是白設，正式生成時提袋照樣自由發揮。現在 `build()` 會讀。

## 不要畫任何真實品牌的商標與包裝

丹丹漢堡、鄭老牌木瓜牛奶這類都是註冊商標，畫面上只出現**無品牌**的食物本身，
品牌名留在文字欄。同理不用各縣市的官方市徽。

## 讀原海報要看「畫」，不是只看「條列」

同一個錯我犯了三次：既有的東西已經對了，我卻自己發明一套去覆蓋。

| 次數 | 我發明的 | 原本就有的 |
|---|---|---|
| 1 | prompt 寫「鳳凰木」 | 資料裡查證過的市花是木棉 |
| 2 | 縣市層級也拿珍奶 | 宜蘭拿三星蔥、金門拿高粱酒，海報本來就在地化了 |
| 3 | 澎湖戴草帽、穿「海藍亞麻」 | 海報上是海星貝殼髮飾、淺藍小白花襯衫 |
| 4 | 宣稱「朱槿是高雄市花之一」，據此把屏東的扶桑花換掉 | 資料裡高雄市花是**木棉花**（official，兩次全民票選）。朱槿是**舊高雄縣**縣花，2010 合併後已非現行 |

第 4 次的形狀跟前三次不同但一樣致命：不是發明造型，是**憑印象斷言一條事實，
沒有去對旁邊那份已經查證過的資料**。`data/counties.json` 就在手邊，
查一行就知道。要引用任何縣市象徵之前，先讀那個檔案。

| 6 | 台北地標寫成陽明山、北投、象山三個自然景觀 | 海報畫面上畫著**台北101**與捷運，我只讀了文字清單 |
| 5 | 「22 張舊海報全都是盤髮」，於是一律改成盤髮 | 基隆（帽下放下）、台北（散落及肩）、花蓮與台東（花冠配放下的長髮）本來就不是盤髮 |
| 7 | CSS 註解寫著「手機淡化已修好」 | `.figure` 的 `opacity:.2` 從來沒生效——視差腳本寫 inline `style.opacity`，永遠蓋過 media query |
| 8 | 從 1× 手機截圖判斷「海報文字壓在人物頭上」，準備整段重排 | 3× 放大後文字完全沒碰到人物，構圖是成立的。真正的碰撞只有句子撞到右下角切換鈕，一行 CSS 就解決 |

## 我的量測值不等於使用者看到的東西

同一天犯了兩次同形狀的錯：

- 線上驗證 22 個縣市，回報「21 個載入失敗」。實際上是我只等 260ms 就去讀
  `naturalWidth`，而底圖從 CDN 首次載入要更久。改用 `waitForFunction` 等
  `img.complete` 之後 0 失敗。**本機的等待時間不能直接搬到線上。**
- 判斷手機版面用 1× 截圖，把糊掉的字讀成「壓在人物身上」，差點把整段重排。
  版面爭議一律用 `deviceScaleFactor:3` 加 `locator.screenshot()` 只截那個元件。

還有一種相反的錯：量到 `1.11` 以為透明度大於 1，其實原始值是 `1.11022e-16`，
是我拿 `.slice(0,5)` 截字串把科學記號切斷了。**數字看起來不合理時，先懷疑
自己的讀法，再懷疑被量的東西。**

第 5 次的形狀：**看縮圖下了通則，再用通則覆蓋掉個案**。
20 張要逐張放大看頭部，不是掃一眼就宣稱「全部都是」。
花冠配放下的長髮是一體的設計，戴在盤髮上會很怪。

## 22 個縣市只有 20 張海報

`新竹市／新竹縣` 共用 `hsinchu-city`、`嘉義市／嘉義縣` 共用 `chiayi-county`。
所以**新竹縣與嘉義縣的造型無海報可讀**，目前是推定的，資料的 `notes` 有標記。
這兩對還必須刻意做出差異，否則兩張圖會長得一樣——
目前靠各自的縣花區分（新竹縣茶花／嘉義縣玉蘭花）。

而且澎湖背景漏掉西嶼燈塔，成因是我只讀海報的**文字清單**（雙心石滬、七美藍洞、天后宮），
沒看海報**畫面上**畫了燈塔、漁港、海鷗——燈塔還是澎湖最強的視覺地標。

**所以每個縣市開工前，先看那張海報的人物區與背景**，把已經對的東西抄下來，
不要自己想。`scene.hair`、`scene.outfit`、`scene.landmarks` 都要這樣填。

## 底圖的美術指導：六條，每條都是踩過坑才有的

`tools/build_prompt.py` 的 `build()` 裡有這六條，改它之前先讀完這節。

**一、一個主地標畫清楚，其餘退到後面。**
原本寫「三個地標都要清楚可見」，那等於要模型把資料表畫成圖——
三個等權重的物件互相競爭，眼睛不知道該看哪。
現在只有 `scene.hero_landmark` 畫得銳利，其餘是配角。

**二、配角要豐富，但全部低對比。**
只寫「其餘退到霧裡」會讓畫面變空。主次靠明度與清晰度拉開，不是靠減少東西。
所以要明講「填滿左邊 60%：屋簷層層、燈籠、兩三個遠處行人、街樹、機車」。

**三、四個景深層，各自指定明度與清晰度。**
沒有景深指令的話畫出來只有「人物」和「背景」兩層。

**四、單一光源，方向寫明。**
`scene.light` 依地理決定：西部夕照、東部晨光、內陸午後斜光。
沒指定時每張的光向都不同，物件看起來像各自貼上去的。

**五、視點。這是最後才發現的漏洞，而且最致命。**
台北那張把陽明山（北邊山上）、北投（山腳）、101（市中心盆地）畫在同一格——
**沒有任何位置能同時看到這三個**，結果 101 從雲海裡獨自聳立、旁邊接一條山城老街。
現在 `scene.viewpoint` 明寫「她站在哪裡」、`viewpoint_sees` 明寫「從那裡看得到什麼」，
並要求畫面裡只能出現從那一點真的看得到的東西。

視點也必須與留白規則相容：花蓮的視點在太魯閣峽谷內，但 `right_zone` 設成 `sea`，
峽谷裡看不到海，四個候選全部被畫成清水斷崖。所以有 `right_zone_desc` 可逐縣市覆寫。

**六、留白是「安靜」，不是「空」。**
這條我一開始就做錯，而且錯得最久。原本要求右側 40% 幾乎無細節，
結果 22 張畫面全都空洞。實測右側最豐富的候選（澎湖 02、新竹市 04、高雄 02）
反而最好看——而文字只要加一層很淡的漸層就完全讀得到。

**可讀性該用合成手段解決，不是靠犧牲畫面。**
`index.html` 的 `.split::after` 就是那層漸層：只壓在文字帶的高度、很淡、
用 `mask-image` 讓上下淡出。整片壓會把右半洗成灰白，畫面層次全失——
那個做法先前被打回過兩次。

## 量測會過期，而且會開始懲罰好圖

`tools/verify_base.py` 的門檻改過三次，每次都是因為它在擋正確的東西：

| 指標 | 原值 | 現值 | 為什麼 |
|---|---|---|---|
| 文字區起伏上限 | 0.030 | **0.055** | 舊值把右側豐富的好候選全判不合格 |
| 人物右緣上限 | 0.58 | **停用** | 它量的是「邊緣密度最右一欄」，右側有內容後量到的是背景不是人物 |
| 縣市名區亮度下限 | 0.55 | **0.42** | 大字＋強白光暈在 0.54 的藍天上實測依然清楚 |
| 右側結構下限 | 無 | **0.010** | 只有上限沒有下限，四張空的一路留到現在 |

### 右側結構下限：只量上限的代價

第六條「留白是安靜，不是空」寫了很久，但**沒有東西在量它**，所以 22 張裡有四張右側是
大片沒有內容的霧，一直沒被發現。現在 `verify_base.py` 量右側 40% 整個高度的
16×16 區塊亮度標準差中位數，低於 0.010 就不合格。

門檻不是猜的：實測最低四張是 0.0031（彰化）、0.0038（台北）、0.0043（南投）、
0.0051（新北），次低的一批從 0.0122 起跳，中間有兩倍以上的斷層。目視也同意。

**教訓的形狀跟前三次一樣：量測是我自己寫的，所以只會抓到我想到的問題。**
我想到了「太雜會讀不到字」，就沒想到「太空會沒有畫面」。

### 右側太空，有一半是規格自己造成的

以為是模型畫不好，查下去發現是 `right_zone` 跟 `viewpoint` 互相矛盾：

| 縣市 | 視點 | 原本要求 | 問題 |
|---|---|---|---|
| 彰化 | 鹿港老街巷弄 | `sea` | 巷子裡看不到海，模型只能糊一片霧 |
| 新竹市 | 城隍廟前廣場 | `sea` | 同上，而且**它真的畫了海與港灣拱橋** |
| 南投 | 日月潭碼頭 | `sky` | 碼頭邊該是湖面，不是開闊天空 |

這跟花蓮那次（視點在太魯閣峽谷內但 `right_zone` 寫 sea）是同一個形狀，
解法也一樣：改對 `right_zone`，並用 `right_zone_desc` 逐縣市寫具體。

**新竹市那張是目視抓到的，量測給它通過**（0.0257，有內容）。
量測永遠不會問「這個場景成立嗎」——那一題只能用眼睛。

### 右側是要設計的區域，不是拿東西填滿

補了右側結構下限之後，我照著那個數字去挑候選，結果從「太空」直接擺盪到「太吵」：
台北那張的右側變成一整片大小相近、對比相同的水泥方盒，一路鋪到右緣，沒有隨距離變淡。
使用者的評語是「看起來未經設計，隨便填了海或房子給我」。

我第一個念頭是再加一個「層次」指標。量了才發現量不出來：

| | 上半（文字帶）細節 | 下半細節 |
|---|---|---|
| 台北（醜） | 0.0045 | 0.0989 |
| 彰化（好看） | 0.0054 | 0.0616 |
| 苗栗（好看） | 0.0090 | 0.0735 |

數字幾乎一樣。**醜不醜跟這些統計無關，那是構圖，不是數值。**
再發明一個我自己想得到的數字，就是第四次犯同一個錯。

真正的差別在於：好看的那幾張右側**有一個明確的主體**（紅磚屋脊層層退去、火炎山礫岩配溪床），
醜的那幾張是一整片同質材質。而查下去發現，好看的那幾張正好都寫了逐縣市的
`right_zone_desc`，醜的那幾張吃的是通用預設——而通用預設寫的是：

> the right 40 percent ... whatever genuinely belongs at this viewpoint

**「凡是那個視點看得到的東西」，意思就是「隨便填」。** 構圖決策被丟給模型了。

所以做法是逐縣市寫 `right_zone_desc`，講清楚**那裡是什麼、由近到遠分成哪幾層、
哪一層最淡**。台北與新北照這個寫法重生之後，0.0038→0.0314、0.0051→0.0312，
而且目視是有景深順序的城市與海，不是同質材質。

**22 個縣市現在全部都有 `right_zone_desc`。** 通用預設仍留著當保險，但正常情況不該再被用到——
新增縣市或新增特別版時，記得一併寫。

不過補完之後有一個發現值得記著：**通用預設在多數情況下其實運作得可以**。
逐張看過 16 個縣市的右側，只有 4 張真的是「同質材質」或「主地標壓進文字區」，
7 張本來就有明確主體與明度分層。所以補規格是為了「不保證」而不是「一定爛」，
**不要因為補了規格就把 22 張全部重生**——重生會冒新的風險，台北那次五個候選一個太吵四個太空。
這次只重生了 5 張。

## 文字帶的底：一層漸層，不要模糊

原本 `.split-top::before` 與 `.split-side::before` 各有一塊橢圓遮罩，加 `backdrop-filter:blur(7px)`。
兩個問題：

- **兩塊橢圓**在中間重疊（濃度加倍）、上下各自收邊，密度不勻
- **模糊被遮罩切斷**：遮罩邊界在哪，「糊」與「清晰」就在哪斷開，那是一條看得見的弧線

改成 `.split::after` 一整片、只用漸層、不做模糊：

- 橫向 28% 起、右緣最濃 0.78；縱向上下都收到完全透明
- 用 smoothstep 而不是線性。線性在起點有斜率突變，眼睛會讀成一道淡邊；
  smoothstep 端點斜率為 0。實測每像素最大變化 0.235 灰階，遠低於 1，不會有帶狀
- **沒有模糊就沒有糊／清晰的邊界可看**

濃度 0.78 是量出來的：25 張裡 22 張的文字對比達標（大字 3.0／內文 4.5），
剩新竹市 4.50、台南 4.42、彰化 3.62，都是最差 5% 的像素，中位數在 10 以上。
0.62 會讓彰化掉到 2.93。

## 桌機版面重現得出來，別誤判成環境限制

我一度回報「這個環境視窗鎖在 500px，桌機版面重現不出來」，那是錯的。
`resize_page` 改不動視窗，但 `emulate` 設 viewport 就可以：

```
mcp__chrome-devtools__emulate  viewport: 1600x1000x1
```

**工具用錯不等於做不到。** 說「做不到」之前先換一個工具試。

## 「查無官方頁面」多半是抓法碰不到，不是真的沒有

上一輪查市花，11 個縣市被寫成「官網查無公開的象徵頁面：首頁無連結、站內搜尋查無、
搜尋引擎亦僅得不相關頁面」。第二輪重查，其中 9 個都找得到，一個都不是真的不存在：

| 縣市 | 上一輪為什麼碰不到 | 這一輪怎麼拿到 |
|---|---|---|
| 桃園 | 縣府站有 WAF，curl 與瀏覽器自動化都回 403 | 象徵寫在《新修桃園縣志──贅錄志》PDF，讀 Wayback 的存檔 |
| 雲林 | Cloudflare 擋 | Wayback |
| 台東 | 對本機 IP 回 Web Page Blocked | Wayback |
| 宜蘭 | 那一頁**原址已經失效**，會轉回首頁 | Wayback |
| 嘉義縣 | 擋 curl | 用瀏覽器讀渲染後的內容 |
| 苗栗、新竹市、花蓮、金門 | 站內搜尋搜不到 | 換關鍵字組合搜尋引擎就出來了，頁面一直都在 |

**「我抓不到」不等於「它不存在」**，跟上一節「工具用錯不等於做不到」是同一個形狀。
判定一項資料查不到之前，至少換過：curl、瀏覽器、Wayback、以及該單位的出版品（縣志、公報、法規）。

同一輪還抓到第二種錯，形狀不一樣但更該記著：**引註存在不等於引註支持**。
維基「台灣各縣市象徵列表」被當成可信是因為「該表附有政府引註」，但逐列開來看：

- 金門那列引的是「認識金門」，而那一頁的「縣花」標題底下印的其實是**縣徽**的說明，
  整頁沒有「四季蘭」三個字（2021 年存檔與現況都一樣）
- 屏東與高雄兩列**根本沒有引註**，卻一起被標成「維基且附政府引註」
- 連江那列的引註是民間論壇轉錄的縣志，不是政府網站

所以升級成 `official` 之前要真的把那一頁讀完並抄一份存進 `data/sources/symbols/`，
不能只看「有沒有附連結」。**沒讀就標，等於把可信度標籤變成裝飾。**

還有第三種，成本最低但最容易犯：**開了那一頁，卻只讀自己要的那一格。**
市樹與市鳥上一輪整批停在維基層級，但查市花時開的官方頁面多半同一頁就寫著
（新竹市那頁三項齊全、彰化「本縣象徵」三項齊全、台南「市府識別」三項齊全）。
這一輪只是把同頁已經寫了的抄下來，官方格數就從 20 格變成 51 格。
頁面既然已經開了，順手把整頁讀完。

## 縮圖上的判斷不算數，服裝細節一律放大看

**這個 session 我在縮圖上判斷錯了四次，四次全部往壞的方向錯，每次都差點把對的圖丟去重抽。**

| 我在縮圖上說 | 放大之後 |
|---|---|
| 客家兩張都是立領盤扣對襟 | 新竹那張是**右開大襟＋扁平直布扣**，完全正確；錯的是苗栗 |
| 新北那張眼睛偏綠（`must_not` 明列） | **暖棕色**，沒問題 |
| 拉阿魯哇片裙沒過膝、排灣裙長只到大腿 | 兩張都**到小腿**，排灣是被前景花叢遮住 |
| 太魯閣的形制像民族風洋裝 | **無袖短衣＋分離套袖**——肩膀裸露正是因為上衣無袖、袖子是獨立的。那正是資料寫的形制 |

中途我還寫過一段用臉部區域色彩統計去驗虹膜顏色的程式，那量到的是膚色，一樣不算數。

**規則：開襟方向、扣件形式、裙長、頭飾、眼睛顏色這一類一兩個像素寬的特徵，
一律裁出那個部位放大到 500px 以上再判斷。縮圖只能用來排序，不能用來否決。**

判斷錯的代價是不對稱的：把錯的當成對的，最多是留下一張要修的圖；
把對的當成錯的，是白燒八分鐘生成、還可能換到更差的候選。



這一輪我在縮圖上判斷錯兩次，兩次都是往壞的方向錯：

- 說「客家兩張都畫成立領盤扣對襟」。放大胸襟之後，新竹那張其實是**右開大襟＋扁平直布扣**，
  完全正確；真正畫成對襟的是苗栗那張。我差點把對的那張也丟去重抽。
- 說「新北那張眼睛偏綠」，`must_not` 明列不得綠眼。放大臉部之後是**暖棕色**，沒有問題。
  中途我還寫了一段用臉部區域色彩統計去驗的程式，那量到的是膚色不是虹膜，一樣不算數。

**開襟方向、扣件形式、眼睛顏色這種一兩個像素寬的特徵，縮圖上看到的是自己的預期。**
要判斷就裁出那個部位放大到 500px 以上再看。這跟先前「用 1× 手機截圖判斷版面」是同一個形狀。

## 文字鎖得住結構，鎖不住比例與分佈——該用實物照就用

噶瑪蘭那一筆的文字依據沒有錯：原民會族群介紹寫「上半部是方衣系統、用兩幅布縫合而成，
下半身則以一塊布料裹身，以黑色、白色為主」，每一項都對得回去。
但那段話**沒有寫黑白怎麼配置**，模型就畫成寬斑馬條紋。實物是白色開襟上衣、
前襟與袖口與下襬鑲黑寬邊、黑邊上綴白珠與銀圓片——差得離譜，而且被使用者一眼看出來。

阿美族那一筆的規格厚得多（female 欄九項逐條），仍然花了六個候選加兩次補規格，
「三片方布」的層次到現在還看不出來。**這是文字這個媒介的上限，不是寫得不夠努力。**

| | 文字擅長 | 文字幾乎無能為力 |
|---|---|---|
| 例子 | 開襟方向、衣長、幾層、扣件形式、什麼不准出現 | 黑白各佔多少、鑲邊多寬、珠飾多密、哪裡留白 |

所以 `scene.costume_refs` 可以逐筆附實物照，`build_prompt.py` 會把它排在參考圖的最後一張。

**收照片有三道關，缺一不可：**

一、**照片的授權**。國家文化記憶庫（`tcmb.culture.tw`）的數位物件是 CC BY-NC 3.0 TW+，
非商業可用、要姓名標示，適合這個專案。全國原住民族文化館主題網（`lcm.tacp.gov.tw`）
頁尾只有 `Copyright © 2022`，沒有開放授權——那批只能當閱讀資料用來寫規格，不進 repo、不進 prompt。
每一張的出處、典藏單位與授權寫在 `data/costume-refs/SOURCES.md`。

二、**智慧創作專用權是另一件事**，跟照片授權無關，兩件都要過。噶瑪蘭已登記
「tibay（女子禮裙）」（噶 GH000026），所以這一筆畫日常常見形制、不畫禮服。

三、**不要把「不准抄紋樣」當成安全牌**。第一版的 prompt 我寫成「不得複製任何織紋」，
那是自相矛盾的：族服的識別特徵常常就是紋樣本身（賽夏沒有雷女紋只剩標籤、
太魯閣沒有菱紋就變泛族風），禁掉紋樣等於禁掉這張圖存在的理由。
要禁的從來不是紋樣這個類別，是兩件具體的事：**畫成登記在案的特定作品**，
以及**加上可辨識到單一部落的專屬紋章**。現在的 prompt 明講「照片上的珠飾與圓片排列就是
這件衣服的重點，要畫」，另外兩件才是否定句。

而且對噶瑪蘭來說，照片是紋樣的**唯一**依據：`costume.json` 的 motifs 欄只記了十九世紀
結婚服飾的挑織紋，現行黑白款那一格是空的。照片上黑邊的白珠小十字，正好對得回記載裡的「米字」。

## 服飾參考照的兩條硬規則，都是被使用者抓到才知道的

**一、參考照不能有臉。** 第一版收的是人物半身特寫（含臉），prompt 也明寫
「不得取照片裡的人、臉、姿勢與背景」。**沒有用**：出圖的臉型變長、眼睛變窄、
看起來老了幾歲，不是阿蕊。而我自己的驗收只對了服飾與場景，**根本沒對臉**，
是使用者先看出來的。

所以現在 `data/costume-refs/` 收的一律是**裁掉頭部的服飾特寫**，
`build_prompt.py` 也在 prompt 裡明講「服飾照是不含臉的裁切圖，臉、眼形、臉型只能來自角色卡，
如果成品的臉認不出是同一個人，這張圖就是錯的」。

**出圖驗收一定要有「跟角色卡並排對臉」這一項**，跟對服飾、對地標同等級。

**二、紋樣種類對了，密度錯了一樣是錯。** 第二個被抓到的問題：噶瑪蘭黑帶上的珠繡，
種類畫對了（十字），但我們畫成連續的白珠鑲邊加密集鑲嵌，而實物是**黑底大量留白、
上面稀疏排著幾個獨立的十字**。看起來就是另一件裝飾密度高得多的衣服。

這跟太魯閣那條「識別特徵是留白，不是織紋密度」是同一形狀，只是換了一族。
現在 prompt 有一條通則：**「照片上稀疏就要畫稀疏；把帶子填滿連續珠線或密集鑲嵌，
即使每個紋樣本身都正確，仍然是錯的。」**

順帶記下那個十字到底長什麼樣，因為寫到這種程度模型才畫得出來：
上下各一條小珠直桿、中央一橫是**三顆圓珠**（○-○-○）、四個端點各收一顆深藍珠，
兩個十字之間夾一小簇 2–3 顆白珠；沿黑帶外緣是**暗灰亮片線**，不是白珠線。
袖口也不是平直的一圈：黑邊包住袖端後，在袖子內側沿開衩**往上延伸成一條有尖端的窄片**。

**寫規格的粒度要到「照著這段能畫出來」為止。** 停在「黑白為主」「有十字紋」那種程度，
剩下的全部由模型自由發揮，而它的預設答案永遠是「更華麗一點」。

## 產出必須可追溯到「這一次請求」

`~/.codex/generated_images` 是整台機器共用的目錄，別的 Claude session 用 codex 也會寫進去。
2026-08-11 到 12 之間，我用三種方式繞過「這張圖是不是這一次產的」這個問題，三次都出事：

| 做法 | 撿到什麼 |
|---|---|
| 直接取最新 session 目錄的最新 PNG | 另一個 session 產的「穿西裝的星空章魚」 |
| 靠 gen.sh 的執行前後比對，但同時跑兩輪 | 自己另一個縣市的圖（太魯閣撿走噶瑪蘭的三張） |
| 服飾參考照裡有真人的臉 | 角色的臉被換掉 |

**而且三次量測都通過**——前兩次撿到的本來就是合格的好圖，只是不屬於這一次。
量測回答「這張圖好不好」，回答不了「這張圖是不是我要的那張」。

現在 `gen.sh` 從 codex 的 stderr 讀 `session id`，只從 `generated_images/<session-id>/` 取圖，
那是唯一能證明歸屬的關聯；另外加 `flock`，一次只准跑一個。
真的要從舊 session 撈回東西時，`~/.codex/sessions/<日期>/rollout-*.jsonl` 裡有那一輪的
`cwd` 與完整 prompt，對得出哪個 session 屬於哪個縣市——2026-08-12 就是這樣把被誤判丟掉的
太魯閣候選找回來的，不必重跑。

## 兩支量測工具說法不一致時，會有人照著錯的那支做決定

`review.py` 的 `measure()` 註解寫著「跟 verify_base.py 同一套判準」，但實際上停在舊門檻：
文字區起伏上限還是 0.030（verify_base 早放寬到 0.055）、右緣 0.38–0.62 還在判定
（verify_base 早已停用，因為它量到的是背景不是人物），而且完全沒量右側結構下限。

後果不是理論上的：太魯閣唯一畫對的那張候選被 `review.py` 判 FAIL，我因此先去看另外兩張，
而那兩張正是混進來的別縣市圖。**改判準要兩支一起改，不然舊的那支會把人帶去看錯的東西。**

## 規格寫「若出現」等於沒寫

賽夏族的雷女紋是它唯一的獨有識別特徵——北賽夏的服裝形制與泰雅極為相似，
沒有雷女紋就真的只能靠標籤區分。我在 `costume_basis` 寫的是
「雷女紋**若出現**必須畫成不對稱的閃電形」，結果四個候選全都沒有雷女紋。

規格沒有錯，但它只約束了「出現時要長什麼樣」，沒有要求它出現。
**識別特徵要寫成「必須出現」，而且要指定畫在哪裡看得到**——
賽夏的雷女紋在長衣背面，正面構圖永遠拍不到，所以還要求至少露一部分在肩上或衣側。

同一輪的鄒族也是：`female` 欄第一項就是黑色頭巾，但畫出來是丸子頭插金珠簪。
現在改成否定句：是纏起來的布，不是髮簪、不是花、不是露出來的髮髻。

## 特別版的服裝要把錯誤版本明確否定掉

第一輪兩張特別版都退回模型熟悉的樣板：卑南族被畫成及地長袍長裙（短身上衣變長袍、
護腿布片被裙子蓋住完全看不見），客家大襟衫被畫成對襟——而對襟正是 `costume_basis`
裡已經標記為錯誤的那一項。

**把正確形制寫清楚不夠，要把錯的那個版本寫成否定句。** 現在 `costume_block()` 固定附一段
「構造不是裝飾」：短身上衣不得變長袍、給了裙長就不得及地、不對稱右開襟不得畫成中央對襟、
直布扣不得畫成打結盤扣、**列出來的每一層都必須看得見**（最後這條就是護腿布片那個坑）。
第二輪卑南族四層全部出現，客家也有候選畫對了。

## 主地標不得與她的手重疊

野柳那次：女王頭是單一小物件，剛好落在她舉起的右手旁邊，看起來像**她手上舉著一塊蜂窩岩**。
七個候選裡有四個都這樣。大範圍地標（老街、城市天際線）不會有這個問題，單一物件一定會。
`build()` 現在明講「地標屬於風景，不屬於她」，要求兩者之間留出背景。

## 換候選不是免費的

四張不合格的，先試過換既有候選，實測都不能用：

- 台北的候選右側有山巒，但**畫面裡沒有 101**，主地標整個不見
- 新北的候選右側是飽和藍海，但左邊畫了**野柳沒有的瀑布與義大利式山城**
- 彰化分數最高的候選畫著八卦山大佛＋芳苑風機＋海，三者不在同一個視野裡，
  而且主地標鹿港老街根本沒出現
- 南投的候選穿的正是要淘汰的那件幾何織紋族服

**只看單一指標挑候選會挑到壞的。** 分數只能用來排序，成不成立還是要一張一張看。

**教訓：量測是我自己寫的，所以只會抓到我想到的問題。**
規格改了，量測要跟著改，否則它會變成阻力。而「主地標夠不夠清楚」「場景成立嗎」
這類問題量測永遠抓不到，只能目視。

## codex 一次會產生多個候選，全部都要看

`$imagegen` 不是「畫一張逐步改進」，是**產生多個不同的嘗試**——實測一次出 2 到 9 張。
`gen.sh` 原本只取最新的一張，等於每次丟掉其餘候選，而且沒有理由相信最後一張最好：
台北那次五張裡最新的 101 最小最淡，第 3 張才是最好的。

現在 `gen.sh` 把全部候選存到 `<輸出檔>.cand/`，用這兩支工具審：

```bash
python3 tools/review.py     # 產生 review/index.html，全部候選並列＋量測＋評語
python3 tools/pick.py 桃園市 04 --note "理由"   # 換上並自動量測、同步、重建審查頁
```

83 張候選逐一比對之後，**16 個縣市的預設選擇被換掉**，全部是同一個原因：右邊有東西的比較好看。

## 生成的圖一定要自己先審過再拿給人看

我犯過的錯：拿到台北那張新圖，只檢查了「主地標有沒有出現、景深有沒有分層、
光線有沒有方向、留白是不是有質感」——四項都達標就宣告成功。
使用者說「你不覺得很奇怪嗎」我才回頭看，發現 101 從雲海裡獨自聳立、
旁邊接一條山城老街，地理上根本不成立。

**成因是我把圖當成規格檢查表在勾，沒有當成一張圖在看。**
規格是我自己定的，只檢查規格等於只檢查自己想到的部分。

所以每張生成的圖，拿給人看之前要跑完這份清單，而且**要寫出檢查結果**，
不能只說「好了」：

**一、這個場景成立嗎**（規格檢查表抓不到的那一類）
- 畫面裡的東西，是不是全部都能從同一個位置看到？
- 主地標所在的環境對嗎？（101 在市區盆地裡，不在雲海上）
- 在地人看得出這是哪裡嗎？還是換個地名也說得通？
- 有沒有明顯不屬於這個縣市的東西混進來？

**二、造型對不對**（逐項對照 data/counties.json）
- **臉：裁出來跟 `data/character/face.png` 並排比**。圓臉、大圓眼、笑法要認得出是同一個人。
  這一項不是形式：2026-08-12 噶瑪蘭那張的臉被服飾參考照裡的真人帶走了（臉型變長、眼睛變窄），
  而我當時對完了服飾、紋樣、地標、量測，就是沒對臉——是使用者先看出來的。
- **雙手：各裁一張放大數手指**。拇指＋四指，沒有融合、沒有多指、握東西的那隻也要看。
  同一天使用者從候選裡撿出兩張手壞掉的，我在挑圖時同樣沒放大看過手。
- 髮型（盤髮／放下，看該縣市的 scene.hair）
- 髮花是不是該縣市的那一種
- 服裝、配件、手持物
- **紋樣要對到「密度」不只是「種類」**：照片上稀疏就要畫稀疏。噶瑪蘭那次每個十字都畫對了，
  但畫成連續珠邊加密集鑲嵌，看起來就是另一件衣服。

**三、規格**
- `python3 tools/verify_base.py <縣市>` 量文字區與人物佔比
- 畫面裡有沒有混進文字
- 手指數、眼睛顏色、有沒有踩到 card.json 的 must_not

**四、跟舊版並排比**，不是單看新版。

## 文字慣例

- 正體中文，對外文案用全形標點
- 不使用 emoji
- 不要寫「XX人的代表」這種宣稱代表性的副標

## 驗證

```bash
cd /home/ct/taiwan-people && python3 -m http.server 8901
```

方形分享圖用到 canvas 讀圖，**必須透過 http 測**；
`file://` 會因跨來源限制讓 `toBlob` 失敗。

改完至少確認：22 縣市可點、無 404、桌機與手機皆無橫向捲軸、無 JS error。

**實物照公開展示之後，授權標示是法律條件不是禮貌。**
`costume-refs.html` 把每一張實物照公開在網站上，其中 12 張是 CC BY-SA 4.0、
還有 CC BY-NC、CC BY-NC-SA、OGDL 等六種授權，姓名標示與相同方式分享都是條件。
所以：**新增實物照一定要同時在 `data/costume-refs/SOURCES.md` 登記**，
然後跑 `python3 tools/build_costume_refs_json.py` 轉出網頁要讀的 JSON。
那支腳本會檢查「檔案在 costume-refs/ 但表格沒登記」並直接中止——
漏登記的照片會以「（未標示）」出現在公開頁上，那是要避免的。
SOURCES.md 仍然是唯一要編輯的地方，JSON 是產生物，不要手改。

## 2026-08-13～16 這一輪的十條教訓

**一、不要在同一張圖上疊加編輯，發現錯就改規格重新生成。**
本機 codex 的自我修正是「拿上一張再編輯」。邵族那輪跑了 10 次局部修正，
把每一次的輸出依時間排出來量縱向條紋振幅：0.235 → 0.242 → 0.282 → 0.303 →
0.326 → 0.358 → 0.397 →（第 8 次是重新生成，掉回 0.255）→ 0.445 → 0.525。
單調遞增，最終版是第一版的 2.2 倍，肉眼看得出整片雨絲感——使用者先發現的。
修形制的代價是畫質累積劣化，而且沒有上限。正確作法是改規格重跑，
`tools/gen_remote.py` 就是這個模式（單發生成＋只看不改圖的 vision 複驗）。

**二、否定句規格沒用，要描述「應該看到什麼」——而且要可數、可指位置。**
到 2026-08-16 為止這招用了四次，每次都是同一個轉換：

  拉阿魯哇  「兩片分開、絕不合併」失敗 → 「中間整條都看得見黑裙」成功
  賽夏      「連續格狀紋」被畫成菱形回紋 → 「四臂各轉一個直角、成方形螺旋」成功
  鄒族      「短上衣不要變成長袍」失敗 → 「由上而下要讀得出四條水平邊界」成功
  北客      「7 對直布釦」只畫出 3 對 → 「喉下 1、大襟斜下 2、腋下轉折 1、脇邊 3」

最後那個特別值得記：**總數是抽象的，位置是可以照著畫的**。
而且國史館的原始描述本來就寫著位置（頷下1、大襟2、腋下1、脇邊3），
是我抄進規格時只留了總數。**照抄來源比自己濃縮安全。**

驗收也一起受惠：可數的東西 vision 答得出來，「有沒有太長」答不出來。

**二之一、原本那條：**
拉阿魯哇的腰帶寫了兩次「兩片分開、絕不合併」，兩次都被畫成一整片。
改成描述中間應該看到什麼——「黑裙在它們之間整條都看得見，每條的兩側都有自己的
自由邊」——一次就對。這樣寫的附帶好處是驗收變成可問的問題：
「兩條之間看不看得到黑裙」有明確答案，「有沒有合併」沒有。

但描述主體還不夠，**也要描述主體之間的關係**。賽夏的雷女紋改成「一個一個數得出來」之後，模型把卍形畫成孤立的個體排在空紅底上——實物上它們是被白色菱格線串連成一整張織網的。少了「相連」這件事，主體就會被孤立出來，既偏離實物，也可能落在會被誤讀的形態。

**三、要量新東西，先拿已知答案的樣本校準；沒校準的指標不准拿來推翻結論。**
使用者說某張圖有疊加紋理，我用「天空區高頻雜訊」量，結論是「跟其他張差不多，
應該是檢視縮放的假影」——錯的。改用 FFT 找週期性，也沒抓到。
最後用 /v1/vision 問，它獨立確認了使用者說的紋理，而且排序跟使用者的感受一致。
更糟的是中途我用一個沒校準的「縱條紋振幅」指標去推翻自己原本正確的推論，
那個指標把最乾淨的噶瑪蘭算成最嚴重。同一天還犯第二次：跨圖比較髮絲量時
沒有按圖寬正規化半徑，得到「v2 最乾淨」的結論，正規化後完全相反。
**跨圖比較的任何指標都要先正規化，並且拿一組已知答案驗過再用。**

**四、假 NG 會讓人開始略過整份清單，而它有四種來源。**
2026-08-13 到 15 每一種都踩過一次：

  男裝條目          阿蕊是女子，鄒族六條裡五條是男裝，第一輪七個 NG 全假
  清單比規格舊       賽夏的清單停在「不對稱閃電」，規格早就改成卍字格狀滿版
  清單跟自己打架     賽夏「白紅黑是唯一色系」對上同一份資料的「綴白鈕扣與銅鈴」
  問畫面上沒有的     阿美的片裙開口、片褲、綁腿都在大腿中段以下，取景根本切掉了

前三種是清單沒跟著資料更新，第四種是**清單問了鏡頭外的東西**。
寫條目的判準是：**對著這張圖看得見嗎？** 看不見的（被裁掉、被外衣蓋住、
屬於另一個性別或年代）就不要列進出圖驗收，寫在 costume_basis 當寫規格時的提醒即可。

**四之一、原本那條：**
`--checklist` 原本會把男裝條目一起列出來，拉阿魯哇那輪十項裡有兩項是這樣來的
假 NG。現在預設濾掉開頭是「男子／男性」的條目，要看全部加 `--all`。
但這還不夠——卑南那輪又出現「用族群整體 palette 去評個別衣件」的假 NG
（palette 說的是刺繡用色，不是整件衣服的主調）。寫 checklist 條目時，
要寫成對著畫面就能回答是或否的問題，不要寫成需要背景知識才能判斷的描述。

**四之二、男裝條目的過濾器只認開頭，而有些男裝條目根本沒寫「男子」。**
拉阿魯哇的「帽上的羽毛是不是左右各一叢分開立起」講的是男子皮帽，
整句沒有出現「男子」二字，`--checklist` 的過濾器（`startswith("男子","男性")`）
自然抓不到，每一輪都混進阿蕊的驗收裡。已補上前綴。

一度想寫成自動檢查（找「句中有男子但開頭沒有」的條目），寫完才發現它
**抓不到實際發生的那個案例**——那條原文根本沒有「男子」二字——
而唯一報出來的鄒族「有沒有畫到男子的獸皮帽」其實是正確的女子條目
（問的是「有沒有誤畫男裝」，出現＝錯）。抓不到目標又只會誤報，所以刪掉了。
**這種要靠語義判斷的條目，目前只能人工審。**
12 個特別版的清單在 2026-08-16 全部審過一輪，結論寫在各族的 `gaps`。


**五、同一份規格裡，同一個部位只能講一次；講兩次就會有一次被忽略。**
2026-08-16 排灣那張出來是漢式立領＋兩顆盤扣的短衫加腰帶圍裙，
跟同一批的兩張客家圖幾乎同款。查回規格才知道不是模型的錯，是規格自己寫壞的：

  領型   參考照那段寫 the standing collar，本體那段寫 a round neck　→ 選了立領
  開襟   只寫 opening to the RIGHT，沒說是直斜線　　　　　　　　　　→ 畫成漢式大襟弧線
  腰     outfit 沒提，accessories 卻要求「腰上一條素布腰帶」　　　　→ 圍裙打結

三處都是同一個部位在兩個欄位裡各講各的。**衝突時模型選的是比較常見的那個先驗**
（漢式立領），不是比較正確的那個。改法照教訓二：寫成五件可數的事
（低圓領無立領、一條直斜襟、布綁帶非釦子、等寬筒袖、下襬過膝所以腰不繫帶），
再加一段反向界線明講「有人能把這張標成客家服飾就是錯的」。

這輪收成 `tools/check_spec_conflicts.py`，改完規格、送去生圖之前跑一次：

    python3 tools/check_spec_conflicts.py

作法是把 outfit／accessories／hair 串起來，用互斥講法的 regex 分組
（領型、固定方式、衣長、袖、腰），同組匹配到兩種以上就報。
否定詞的偵測窗口是**往前到最近的句號或換行**，不是固定字數，
也不能把分號當句子邊界——禁止項常寫成「FORBIDDEN: a; b; c; d」，
用固定窗口的話排在後面的項目看不到那個 FORBIDDEN，整串會被誤報成主張。

**校準這件事我連錯兩次，兩次都是負控制設計錯：**
第一次拿 git HEAD 當負控制，但那段矛盾文字還沒 commit，HEAD 裡根本沒有，
「改前改後都乾淨」的結果毫無意義。第二次改成把排灣的「圓領」替換回「立領」，
可是那是文字裡唯一一處圓領，替換等於把兩種講法變成一種，當然不報矛盾。
**負控制要「加入」一個競爭主張，不是「換掉」原有的那個。**
而且短字串通過不代表真的能用——同一份程式在短字串上過關、
在幾千字的真 outfit 上什麼都抓不到。`--self` 現在兩種都驗。

**六、我判「圖畫對了」而 vision 判 NG 時，先假設 vision 是對的。**
排灣那張我看過一遍，說「素面、右開襟、筒袖、衣長過膝，照著規格畫對了，
五個 NG 全是假的」，然後才去改清單。實際上真正錯的是圖：立領、盤扣、
衣長只到腰、腰上還綁了圍裙。我把「符合我心裡的規格」誤讀成「符合畫面」。
前四條教訓都在講假 NG，反過來的情況一樣會發生——
**先前踩過假 NG，會讓下一次真 NG 也被當成假的。**
判定假 NG 的門檻是：指出畫面上實際畫了什麼、而清單要求的是畫面外或另一個設定的東西。
說不出「畫面上實際是什麼」就不能判假。

**七、每一輪的輸出都要留檔，不要直接覆蓋 `img/`。**
排灣跑到第二輪時形制全對了（低圓領、直斜襟、布綁帶、筒袖、過膝、腰無帶、
A 型開叉露出片裙、護腳布），只差顏色偏深與髮花形狀；第三輪改完規格重跑卻倒退
——上衣縮回臀部、開叉不見了。這時才發現第二輪那張已經被第三輪覆蓋掉，
`/tmp/taiwan-people-gen/` 的 PNG 也是同一個檔名，一起沒了，
git 裡留的是第一輪的錯版。等於白跑一輪。

**同一份規格每次抽樣本來就會有好有壞，單次結果不能拿來判斷規格改對沒有。**
正確作法是連跑三輪、每輪存成不同檔名，跑完一起看再挑，
挑中的那張才複製進 `img/`。
收成 `tools/gen_rounds.sh`。

**這支第一次跑兩輪全空，而我差點沒發現。**原因是它漏了撈 `CODEX_IMAGE_KEY`
（`~/.bashrc` 第 15 行左右有「非互動 shell 直接 return」，排在那之後的 export
對腳本無效，所以不能只靠 source），而我把輸出 `grep -E '═══|✔|✘|NG|OK'` 過濾了，
真正的失敗訊息「FAIL: 沒有 CODEX_IMAGE_KEY」剛好一個關鍵字都不符，
畫面上只剩兩行分隔線，看起來像跑完了。
**新工具第一次跑要看完整輸出，不要先套 grep**——
過濾器是照著「成功時會印什麼」寫的，失敗訊息從來不在那份清單裡。
現在缺金鑰會立刻 exit 1，不再空跑三次重試。

**八、prompt 加長會稀釋前面的指令，改規格時要留意總長度。**
排灣第三輪比第二輪唯一的差別是我把顏色和髮花的描述寫得更仔細，
prompt 從 13658 字長到 14468 字，結果**形制退步**——加長的是顏色和髮花，
退步的卻是衣長和開叉。這跟先前 4:3 指令擺在 11k 字尾端會失效是同一件事：
不是那段寫得不好，是整體變長之後每一段的份量都被稀釋。
髮花那段從 916 字縮回 323 字，只留「跟同一張圖前景那叢是同一種植物」這個可比對的錨點。
**要加細節時，先想能不能把別處刪掉同樣的量。**

**九、矛盾不只出現在資料裡，也出現在「通用段落 vs 族別段落」之間。**
排灣連跑三輪都畫錯，我一直在改 `counties.json` 的族別規格。實際上
`build_prompt.py` 有一段給「所有有實物照的縣市」共用的文字，寫著

    That patterning is the point of the garment, not an optional extra: draw it.
    Match the DENSITY of the ornament in the photographs…

而排灣的族別規格寫的是 Take NOTHING of the decoration。**兩段在同一份
prompt 裡正面打架**，而 `tools/check_spec_conflicts.py` 掃不到——它只讀資料檔，
這一段在程式碼裡。

那段最後其實有一句「照片與文字不符時以文字為準」，但它擋不住：
那是元規則，位置又在整段最後，份量遠不如前面那句語氣強烈的 draw it。
**元規則抵不過具體指令**，尤其當具體指令排在前面。

修法是在 `scene` 加 `costume_refs_cut_only: true`，`build_prompt.py` 據此
整段換成「只取剪裁與比例，紋飾與顏色一律不取，因為那張照片是不同階級或場合的」。
判斷什麼時候要開這個旗標：**照片跟這一筆的階級、場合或年代對不上時**。

**這個旗標還沒有被真正驗證過。** 開旗標之後跑的那一張形制正確，
但採用的那一張是開旗標**之前**跑出來的、本來就畫對了——
兩張都對，證明不了旗標讓事情變好，只證明它沒讓事情變糟。
要真的驗它，得在還會畫錯的狀態下開關對照。目前它的價值是移除矛盾本身。

**排查順序的教訓：出圖一直錯同一個地方，先把整份 prompt 印出來從頭讀一遍，
不要只讀自己剛改的那幾行。** 這個矛盾在 prompt 裡看得一清二楚，
而我改了三輪族別規格才想到去讀它。

**十、措辭改到極限還是不收斂時，那是模型的能力限制，換方法不要換措辭。**
排灣連跑五輪，階級正確性每輪都對（素面、比頭髮淺的炭灰、片裙不是紅、
無貴族紋樣、腰間不繫帶），但這三件事**五輪一次都沒成功**：
前襟該是一條從喉下到右腋的直斜線（每輪畫成中式盤扣式正面開襟）、
該用布綁帶（每輪畫成盤扣）、衣脇 A 型開叉該露出片裙與護腳布（每輪只有同色下身）。

規格那邊能做的都做了：五件事逐一編號、明文列 FORBIDDEN、加反向界線
「有人能把這張標成客家服飾就是錯的」、參考照那段改成只取剪裁。
教訓二那招（把否定句換成可數、可指位置的描述）用到極致仍然不動。

**判準是這樣：同一件事改過三種以上寫法、跑過五輪還是同一個錯，
就不要再改措辭了。**下一步該換維度——換一張階級對得上的實物照，
或把形制單獨畫成線稿當參考圖餵進去。純文字描述形制有天花板。


## 生圖

`~/.claude/skills/codex-imagegen/codex-imagegen.sh` 這支包裝腳本會間歇性失敗
（回報失敗但圖其實已產出，或完全不產出）。所以這個 repo 走自己的 `tools/gen.sh`。

**不要自己去 `~/.codex/generated_images/<最新 session>/` 撿最新的 PNG。**
那個目錄是這台機器上**所有** codex 用途共用的，不是這個專案專用。
2026-08-11 我照這個做法撿圖，撿到的是另一個 session 產的「穿西裝的星空章魚配電腦桌前的女孩」，
差一點就當成阿美族特別版的候選存進 repo。`gen.sh` 執行前後各拍一次 PNG 清單、
只認新出現的檔案，正是為了防這件事——繞過它就會中招。

**背景執行時 codex 會永遠等 stdin。** 這是 2026-08-11 卡掉整個下午的真正原因，
一開始我以為是 codex 慢或額度用盡，兩個都不是：

```
$ codex exec -- "回覆 OK"          # 前景：stdin 已關 → 幾秒回
$ (同一行放到背景跑)                # 印一行「Reading additional input from stdin...」
                                    # 然後停在那裡不動
```

`codex exec` 會把 stdin 當成「還有補充輸入要讀」並等 EOF。前景執行時 stdin 已經關了，
所以看不出問題；丟到背景跑時那是一條永遠不關的管線，它就一路等下去——
實測兩輪分別卡了 1 小時 55 分與 1 小時 27 分，**連 session 檔都沒建**（它根本還沒開工），
而 `gen.sh` 當時又把輸出全丟進 `/dev/null`，所以連那行提示都看不到。

對照實驗（各跑一次，背景）：繼承 stdin → 100 秒被 timeout 砍掉；`< /dev/null` → 10 秒完成。
所以 `gen.sh` 現在固定 `</dev/null`，輸出也留檔不再丟掉。

**另外一種失敗長得很像但不是同一件事**：codex 會回
`Selected model is at capacity`（`codex_error_info: server_overloaded`），
那一輪大約一分鐘就結束、不產圖。`gen.sh` 舊版把這種情況一律報成
「prompt 被拒或額度用盡」——那是猜的，而且猜錯：查 codex 自己記的 `rate_limits`，
週配額只用了 9%、`rate_limit_reached_type` 是 null。現在改成去讀 session 檔裡的真實錯誤。
**猜測性的錯誤訊息比沒有訊息更糟，它會把人帶去查錯方向。**

批次生成仍然要包 `timeout`（伺服器端塞住時還是可能久等）：

```bash
timeout 900 bash tools/gen.sh "$out" "$prompt" $refs || continue   # 卡住就跳下一輪
```

還有一種假成功：gen.sh 回報 OK，但存出來的檔案 md5 與上一輪一模一樣。
候選存檔用內容雜湊當檔名（`r${round}-$(md5sum "$f" | cut -c1-8).png`），
重複的自然不會多存一份，也就看得出來這一輪其實沒產出新東西。

去背一律用色鍵，不要跟模型要透明背景（它會給你綠幕）。
綠幕會撞到畫面裡的綠色物件，用**洋紅**比較安全。
去色邊只作用在半透明的邊緣像素，整張套會毀掉畫面裡本來就有的該顏色。

## 手機不是把桌機縮小

第 2 段原本是「同一份版面，換一組尺寸」。實測 390px 螢幕上地圖的可點區：

| 縣市 | 可點區 | 對 44×44 建議值 |
|---|---|---|
| 嘉義市 | **13×10 px** | 面積 1/17 |
| 基隆市 | 19×15 px | 1/7 |
| 新竹市 | 17×17 px | 1/6 |
| 台北市 | 23×30 px | 1/3 |

22 個裡 4 個按不準。**這不是「小一點」，是「不能用」。**
而且拇指沒有 hover、也不適合瞄準——互動模型本身就不該一樣。

現在手機切的是模型不只是版面：卡片全出血、左右滑動換縣市，
要跳去特定地方才開全螢幕地圖，那時每個縣市補一個 48px 的透明圓命中區，
返回鍵＝關閉面板。桌機一行都沒動。

判斷準則：**改手機版之前先量可點區與可讀性，不要先調 CSS 尺寸。**
量出來若是「數量級的差距」，該換的是互動方式。

### 三個踩過的坑

- **圖片拖曳會吃掉 pointerup。** 滑動手勢完全沒反應，`pointerdown` 有進來、
  `pointerup` 沒有——在 `<img>` 上拖曳啟動了瀏覽器原生的圖片拖放。
  要 `-webkit-user-drag:none` 加 `draggable="false"` 才成立。
- **`visibility:hidden` 會連 ::before 一起隱藏。** 量「純背景」時把文字容器
  設成 hidden，結果薄紗（住在該容器的 ::before）也不見了，量到的是沒有薄紗的原圖，
  兩次量測數字一字不差還以為是快取。要藏就藏「子元素」：`.x>*`。
- **`display:none` 的元素 `getBBox()` 全部回 0。** 靠面積排序疊放順序的程式
  在手機上等於沒排（地圖平時是 none）。改成面板開啟、真的有版面時才排。

### 兩個反覆咬人的 CSS 形狀

各自已經發生兩次，看到就先懷疑這兩件事。

**一、`height` 固定＋`width:auto`＋全域 `img{max-width:100%}` ＝ 圖被壓扁。**
只要容器被擠窄（絕對定位的 shrink-to-fit、flex item 的 flex-shrink），
寬就被夾住而高不變。發生過：首頁人物（390px 上橫向少 56%）、
導覽列 logo（390px 少 43%、360px 少 63%）。
修法一律是 `max-width:none` 再加「別讓容器被擠窄」（`width:max-content` 或 `flex:none`），
兩個要一起，缺一個沒用。

改完跑一次全站比例稽核：對每張 `document.images` 比 `getBoundingClientRect`
與 `naturalWidth/naturalHeight` 的比值，偏差超過 1.5% 就是被壓扁
（`object-fit:cover/contain` 的要排除，那是刻意裁切）。

**二、同權重時由「順序」決勝，media query 不加權重。**
`@media` 裡的 `.x{...}` 跟外面的 `.x{...}` 權重相同，寫在後面的贏。
發生過：`.poster .steps` 的手機覆寫（放在基準規則之前，完全沒生效，
先前那次「移到左下」其實是意外奏效——基準規則剛好沒設 `left`）、
`#about::before` 的 `background-size`（覆寫夾在兩個定義中間，
只有 `#explore` 吃到，`#about` 被後面的 `background` 簡寫重設回去）。

**手機覆寫一律放在被覆寫的規則之後**，而且 `background` 簡寫會重設
`background-size`／`background-position`，覆寫要留意這點。

## 同名檔換內容，回訪者永遠看不到

`sw.js` 對圖與音走 cache-first，註解寫著「內容換了就換檔名（底圖本來就是這個慣例）」，
`ASSET_V` 因此設計成手動 bump。**那個慣例沒有被遵守。**

2026-08-13 查到：`img/*-base.webp` 這輪至少 12 個檔沿用原檔名換掉內容
（噶瑪蘭、太魯閣、賽夏、阿美、排灣、泰雅、屏東、宜蘭，加上首圖兩次），
而 `ASSET_V` 從建站的那次 commit 起一次都沒動過。
結果是：所有裝過這個 PWA 的人看到的都還是舊圖，包含今天修掉的那些服飾錯誤。
線上檔案是對的、`curl` 也是對的、無痕視窗也是對的——只有真正的回訪者是錯的，
所以本機怎麼測都測不出來。

現在 `ASSET_V` 由 `tools/build_sw.py` 依「版控裡所有圖與音檔的內容」算出來，
跟 `SHELL_V` 一樣不由人寫。代價是任何一張圖換內容，離線包整份重抓。

**推論**：凡是「靠人記得改版號」的機制都會壞，而且壞得沒有徵兆。
驗收「線上是不是真的換了」不能只 `curl` 檔案，要用**帶著舊 SW 的瀏覽器**開。
